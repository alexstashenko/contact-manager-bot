# 📇 Contacts Manager

Система для централизации контактов с Telegram-ботом и ИИ-интерфейсом для нетворкинга.

## ✨ Возможности

- 📱 **Telegram-бот** для управления контактами прямо из мессенджера
- 🤖 **ИИ-запросы** через Gemini API (например: "Кто у меня есть из HR?")
- 📝 **История взаимодействий** с автоматическим определением типа (встреча, звонок, покупка)
- 🔍 **Поиск** по имени, компании, тегам
- 📊 **Статистика** по контактам и взаимодействиям
- 📥 **Импорт** из vCard, CSV, JSON

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd contacts-manager
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Получение API ключей

#### Supabase (база данных)

1. Перейдите на [supabase.com](https://supabase.com)
2. Нажмите "Start your project"
3. Создайте аккаунт (можно через GitHub)
4. Нажмите "New project"
5. Заполните:
   - Name: `contacts-manager`
   - Database Password: придумайте сложный пароль
   - Region: Europe (Frankfurt)
6. Нажмите "Create new project" (займёт ~2 минуты)
7. После создания:
   - Перейдите в раздел **Settings** → **API**
   - Скопируйте `URL` (Project URL)
   - Скопируйте `anon public` ключ

#### Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например: "My Contacts Manager")
4. Придумайте username (должен заканчиваться на `bot`, например: `mycontacts_manager_bot`)
5. Скопируйте полученный токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### Google Gemini API Key

1. Перейдите на [aistudio.google.com](https://aistudio.google.com)
2. Войдите в Google аккаунт
3. Нажмите "Get API key" → "Create API key"
4. Скопируйте ключ

### 3. Настройка переменных окружения

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Заполните все значения:

```env
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш-anon-ключ
TELEGRAM_BOT_TOKEN=ваш-токен-от-botfather
GEMINI_API_KEY=ваш-gemini-ключ
```

### 4. Создание таблиц в Supabase

1. Откройте ваш проект в Supabase
2. Перейдите в **SQL Editor** (левое меню)
3. Нажмите "New query"
4. Скопируйте содержимое файла `database/schema.sql`
5. Вставьте в редактор
6. Нажмите "Run" (или Cmd+Enter)
7. Убедитесь, что появилось сообщение "Success. No rows returned"
8. Перейдите в **Table Editor** - должны появиться таблицы `contacts` и `interactions`

### 5. Запуск бота

```bash
cd bot
python main.py
```

Должно появиться сообщение: `🤖 Бот запущен!`

### 6. Первое использование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Нажмите "Start" или отправьте `/start`
4. Попробуйте добавить тестовый контакт:

```
/quick Иван Петров, TechCorp, HR Manager, ivan@tech.com, @ivan_hr
```

5. Добавьте заметку:

```
/note @ivan_hr Познакомились на конференции AI Summit
```

6. Попробуйте ИИ-запрос (просто напишите боту):

```
Кто у меня есть из HR?
```

## 📖 Команды бота

### Добавление контактов

**Быстрое добавление одной строкой:**
```
/quick Имя Фамилия, Компания, Должность, email@example.com, @telegram
```

Минимально достаточно только имени:
```
/quick Иван Петров
```

**Интерактивное добавление:**
```
/add
```

### Работа с заметками

```
/note @username Текст заметки
/note email@example.com Созвонились, обсудили проект
```

Бот автоматически определит тип взаимодействия:
- **встреча** - если в тексте "встреча", "кофе", "обед", "конференция"
- **звонок** - если "звонок", "позвонил", "созвонились"
- **покупка** - если "купил", "оплатил", "продал" (+ извлечёт сумму)
- **email** - если "email", "письмо", "написал"

### Поиск

```
/find Иван          # поиск по имени
/find TechCorp      # поиск по компании
/find HR            # поиск по тегу
```

### Просмотр

```
/list               # последние 10 контактов
/stats              # статистика
```

### ИИ-запросы

Просто напишите вопрос боту (без команды):

- `Кто у меня есть из HR?`
- `С кем я познакомился на AI Summit?`
- `Покажи всех, с кем я не общался больше месяца`
- `Кто покупал у меня в ноябре?`

## 📥 Импорт существующих контактов

### Из приложения Контакты (macOS)

1. Откройте приложение "Контакты"
2. Выберите контакты (Cmd+A для всех)
3. File → Export → Export vCard...
4. Сохраните как `contacts.vcf`
5. Импортируйте:

```bash
python scripts/import_contacts.py ~/Downloads/contacts.vcf
```

### Из CSV файла

Создайте CSV с колонками: `name`, `company`, `position`, `email`, `telegram`, `phone`, `tags`

Пример `contacts.csv`:
```csv
name,company,position,email,telegram,tags
Иван Петров,TechCorp,HR Manager,ivan@tech.com,@ivan_hr,"HR, рекрутинг"
Мария Иванова,StartupX,CEO,maria@startup.com,@maria_ceo,"стартапы, инвестиции"
```

Импорт:
```bash
python scripts/import_contacts.py contacts.csv
```

### Из JSON файла

Формат JSON:
```json
[
  {
    "name": "Иван Петров",
    "company": "TechCorp",
    "position": "HR Manager",
    "email": "ivan@tech.com",
    "telegram": "@ivan_hr",
    "tags": ["HR", "рекрутинг"]
  }
]
```

Импорт:
```bash
python scripts/import_contacts.py contacts.json
```

## 🔧 Структура проекта

```
contacts-manager/
├── database/
│   └── schema.sql           # SQL схема для Supabase
├── bot/
│   ├── main.py             # Основной файл бота
│   ├── handlers.py         # Обработчики команд
│   └── ai_interface.py     # Интеграция с Gemini API
├── scripts/
│   └── import_contacts.py  # Импорт контактов
├── .env.example            # Шаблон переменных окружения
├── .gitignore
├── requirements.txt        # Python зависимости
└── README.md
```

## 💡 Советы по использованию

### Теги для категоризации

При импорте или ручном добавлении используйте теги для категоризации:
- `HR`, `рекрутинг`
- `инвестор`, `стартапы`
- `разработка`, `дизайн`
- `клиент`, `партнёр`

### Регулярное добавление заметок

Заведите привычку сразу после встречи/звонка добавлять заметку:
```
/note @username Обсудили сотрудничество, договорились созвониться через неделю
```

### ИИ для анализа

Используйте ИИ для регулярного анализа:
- `С кем я давно не общался?` - чтобы не терять контакты
- `Кто покупал в прошлом месяце?` - для анализа продаж
- `Кто был на конференции X?` - восстановить контекст

## 🐛 Решение проблем

**Бот не запускается:**
- Проверьте, что все переменные в `.env` заполнены
- Убедитесь, что виртуальное окружение активировано: `source venv/bin/activate`
- Проверьте логи на ошибки

**Ошибка подключения к Supabase:**
- Убедитесь, что URL и ключ скопированы правильно (без пробелов)
- Проверьте, что таблицы созданы (Table Editor в Supabase)

**ИИ не отвечает:**
- Проверьте GEMINI_API_KEY
- Убедитесь, что в базе есть контакты

**Дубликаты при импорте:**
- Скрипт автоматически пропускает дубликаты по email или telegram

## 📝 Лицензия

MIT

## 🤝 Поддержка

Если возникли вопросы или проблемы, создайте issue в репозитории.

---

**Приятного нетворкинга! 🚀**
